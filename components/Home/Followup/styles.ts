import classNameAdder from 'utils/classNameAdder'

const styles = {
    container: "sticky-container flex flex-col justify-between items-start hidden md:fixed"
}


export default classNameAdder(styles)